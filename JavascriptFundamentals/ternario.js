//Operador Ternário

const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado';

console.log(resultado(7.1));
console.log(resultado(6.1));

console.log(typeof resultado);

const status = nota >= 7 ? 'Aprovado' : 'Reprovado';

/* O Operador Ternário é composto por 3 partes
- A expressão que vai retornar verdadeiro ou falo
- A primeira ação caso o resultado seja verdadeiro
- A Segunda ação para caso o resultado seja falso */

/*  */